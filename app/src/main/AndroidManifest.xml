<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lesson1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".color.MainChangeColorActivity" />

        <activity android:name=".color.ChangeColorActivity" />
        <activity android:name=".gameInterface.Records" />
        <activity android:name=".gameInterface.Settings" />
        <activity android:name=".gameInterface.NewGame" />
        <activity android:name=".gameInterface.First" />
        <activity android:name=".Theme14Activity" />
        <activity android:name=".Theme14QuestionnaireActivity" />
        <activity android:name=".Theme16" />
        <activity android:name=".Theme22">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>